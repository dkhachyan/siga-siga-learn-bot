"""Фоновые процессы (§9).

Их два, и разделены они не по объёму работы, а по цене ошибки:

* `scheduler` — раз в сутки в 03:00 по времени человека: отбор слов по SRS,
  один вызов `R3` на все эпизоды дня, раскладка слотов по окну. Медленный,
  дорогой, ходит в модель. Проспать тик здесь — потерять день.
* `sender` — раз в 30 секунд разбирает `scheduled_events`, отправляет готовые
  реплики и напоминания, закрывает эпизоды по TTL. Быстрый, дешёвый, в модель
  не ходит вовсе. Проспать тик здесь — опоздать на полминуты.

Отсюда и разные контейнеры: перезапуск отправщика не должен ждать, пока
планировщик договорит с моделью.
"""
