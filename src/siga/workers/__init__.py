"""Фоновые процессы.

По §9 спецификации их два, и появятся они на этапе 3:

* `scheduler-worker` — раз в сутки в 03:00 по времени пользователя: отбор слов
  по SRS, генерация фреймов эпизодов, раскладка слотов на день;
* `sender-worker` — раз в 30 секунд разбирает `scheduled_events`, отправляет
  открывающие реплики и напоминания, закрывает эпизоды по TTL.
"""
